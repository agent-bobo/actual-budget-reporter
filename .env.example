# Budget Reporter 环境变量配置
# 复制为 .env 后填写真实值

# Actual Budget 连接配置 (必需)
ACTUAL_PASSWORD=your_actual_password_here
ACTUAL_BUDGET_ID=your_budget_sync_id_here

# Gemini API (可选，用于生成智能摘要)
# 如果不设置，会生成基础报告 (无 AI 洞察)
GOOGLE_GENERATIVE_AI_API_KEY=your_gemini_api_key_here

# Discord Webhook URL (必需，用于推送报告)
# 在 Discord 服务器设置 -> Integrations -> Webhooks 中创建
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/xxx/yyy

# 月度预算配置 (可选，JSON格式)
# 单位：分 (cents)，如 50000 = $500
# 配置后会在报告中显示预算健康度
MONTHLY_BUDGET={"餐饮": 50000, "交通": 20000, "购物": 30000}
